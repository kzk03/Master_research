services:
  app:
    container_name: two_tower
    image: two_tower
    deploy:
      resources:
        limits:
          cpus: '20'
    build:
      context: .
      args:
        UID: ${UID}
        GID: ${GID}
        USERNAME: ${USERNAME}
    volumes:
      - ./:/app/
    stdin_open: true
    tty: true